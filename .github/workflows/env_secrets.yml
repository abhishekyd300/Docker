name: Environment Secrets
on:
  #push
  workflow_dispatch

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Say hello
        run: echo "Hello from the GitHub Actions workflow!"

      - name: Print Environment Secrets
        env:
            APP_ENV: dev
            DB_PASSWORD: demo@123
        run: echo "Environment $APP_ENV, Database Password- $DB_PASSWORD"    
          
