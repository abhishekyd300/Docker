name: CI

on:
  push:
    branches:["working"]
  pull_request:
    branches:["working"]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:

      - name: Hello World Action             
        run: echo "Hello from the GitHub Actions workflow!"
